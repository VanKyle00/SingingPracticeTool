# SingingPractice sidecar — direct dependencies.
#
# The basic-pitch group is split out because basic-pitch 0.4.0 pins
# `tensorflow<2.15.1` (no Python 3.13 wheels) and `resampy<0.4.3` (also broken
# on Py 3.13). We install it with --no-deps and supply the deps ourselves.
# See the README for the full install sequence.

# Core: stem separation + YouTube download.
demucs>=4.0.1
yt-dlp>=2026.3.17
soundfile>=0.13.0
numpy>=2.0

# Required by torchaudio 2.11+ for any WAV/FLAC IO that goes through torchaudio
# (we currently bypass it, but installing torchcodec keeps demucs internals happy).
torchcodec>=0.11.0

# basic-pitch + its real runtime deps (resolved by pip).
# Note: we use --no-deps on basic-pitch itself; the package's pinned deps don't
# build on Py 3.13. Versions below are what the project was verified against.
librosa>=0.11.0
pretty_midi>=0.2.11
mir_eval>=0.8.2
scikit-learn>=1.5
onnxruntime>=1.20
mido>=1.3.0
six>=1.17.0
importlib_resources>=6.0
numba>=0.60
lazy_loader>=0.4
pooch>=1.8
msgpack>=1.1
audioread>=3.1
soxr>=0.5
decorator>=5.0
joblib>=1.4
threadpoolctl>=3.5
resampy>=0.4.3   # NOT <0.4.3 as basic-pitch pins
scipy>=1.13
flatbuffers>=24.0
protobuf>=5.0
